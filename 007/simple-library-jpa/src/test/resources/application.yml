spring:
  datasource:
    schema: classpath:database/schema.sql
    data: classpath:database/test-data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

    properties:
      hibernate:
        format_sql: true


